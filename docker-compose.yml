version: '3.8'
services:
  deezer-addon:
    image: ghcr.io/2t0m/deezer-eclipse-addon:latest
    container_name: deezer-eclipse-addon
    restart: unless-stopped
    environment:
      - DEEZER_ARL=${DEEZER_ARL}
      - API_KEY=${API_KEY}
      - PORT=3000
    ports:
      - "3000:3000"